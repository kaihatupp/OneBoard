# data/

スマホ版へ配信する暗号化データを置くフォルダ。

- `oneboard.enc.json` … PC 版アプリの「データ」モーダル →「暗号化して書き出し」で
  ダウンロードされるファイル。ここに置いて `git push` するとスマホに反映される。
  中身は AES-GCM 暗号文で、パスフレーズが無いと読めない(GitHub 上でも中身は非公開)。

初回はまだ存在しない。PC で最初に書き出したものを置くと有効になる。
